# Recomendado: use pnpm (o time do Payload usa internamente)
#   npm install -g pnpm
#   pnpm install
#
# O Next 15.3.9 está dentro do peer range do Payload 3.84.1
# mas o pnpm pode exigir aprovação de builds nativos:
#   pnpm approve-builds  (selecione sharp, esbuild)
legacy-peer-deps=true
